# SOLUCIÓN AL EJERCICIO 3
# Desempaqueta la tupla `coordenadas = (45.5, -74.2)` en las variables `latitud` y `longitud`, e imprímelas.

coordenadas = (45.5, -74.2)
latitud, longitud = coordenadas
print(f'Latitud: {latitud}, Longitud: {longitud}')

# Explicación: El desempaquetado asigna los valores de la tupla a las variables respectivas automáticamente.
